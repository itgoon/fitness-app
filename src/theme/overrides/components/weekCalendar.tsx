import { Theme } from '@mui/material/styles';

// ----------------------------------------------------------------------

export function weekCalendar(theme: Theme) {
  return {
    MuiBox: {
      styleOverrides: {
        root: {}
      }
    }
  };
}
