import * as Auth from './authStore';
import * as Code from './codeStore';
import * as Me from './meStore';
import * as Layout from './layoutStore';

const Store = {
  Auth,
  Code,
  Me,
  Layout
};

export default Store;
